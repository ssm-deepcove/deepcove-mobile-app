import 'package:flutter/material.dart';

class NoticeView extends StatelessWidget {
  final int noticeId;

  NoticeView({@required this.noticeId}) : assert(noticeId != null);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}